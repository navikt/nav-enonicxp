apiVersion: nais.io/v1
kind: Naisjob
metadata:
  labels:
    team: personbruker
  name: xp-navno-deploy
  namespace: personbruker
spec:
  image: ghcr.io/navikt/nav-enonicxp/curlimages_curl:latest
  command:
    - curl
    - https://www.nav.no
  accessPolicy:
    outbound:
      external:
        - host: www.nav.no
