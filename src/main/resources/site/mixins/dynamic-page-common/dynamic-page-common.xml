<mixin>
    <form>
        <input type="TextArea" name="description">
            <label>Oppsummering/ingress</label>
            <help-text>Vises i lenkepaneler når denne siden lenkes til fra andre sider, og i søkeresultater</help-text>
            <config>
                <max-length>140</max-length>
            </config>
        </input>
        <mixin name="languages-legacy"/>
        <mixin name="decorator-toggles"/>
        <mixin name="seo"/>
        <mixin name="keywords"/>
    </form>
</mixin>
