#import "../../richText/richTextFragment.graphql"

fragment partComponentTrekkspill on Part_no_nav_navno_trekkspill {
    trekkspill {
        title
        anchorId
        html(processHtml: { type: server }) {
            ...richText
        }
    }
}
