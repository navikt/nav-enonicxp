#import "../contentCommonFragment.graphql"
#import "../components/parts/partHtmlArea.graphql"

fragment formDetailsTargetFragment on portal_Fragment {
    ...contentCommon
    components {
        part {
            descriptor
            config {
                ... on PartComponentDataConfig {
                    no_nav_navno {
                        html_area {
                            ...partComponentHtmlArea
                        }
                    }
                }
            }
        }
    }
}
