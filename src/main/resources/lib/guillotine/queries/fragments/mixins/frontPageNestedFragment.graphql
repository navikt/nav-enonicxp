#import "../contentCommonFragment.graphql"
#import "./animatedIconsTargetFragment.graphql"

fragment frontPageNestedMixin on no_nav_navno_FrontPageNested {
    ...contentCommon
    dataAsJson
    data {
        illustration {
            ...animatedIconsTargetMixin
        }
    }
}
