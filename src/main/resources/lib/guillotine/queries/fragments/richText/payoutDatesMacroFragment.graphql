#import "../content-types/payoutDatesFragment.graphql"

# This needs to be in a separate file to prevent cyclic imports with payoutDatesFragment
fragment payout_datesMacro on Macro {
    config {
        payout_dates {
            payoutDates {
                ...contentPayoutDates
            }
        }
    }
}
