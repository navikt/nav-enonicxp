#import "../mixins/languagesFragment.graphql"
#import "../mixins/animatedIconsTargetFragment.graphql"

fragment contentGenericPage on no_nav_navno_GenericPage {
    dataAsJson
    data {
        audience {
            _selected
            provider {
                provider_audience
            }
        }
        languages {
            ...languagesMixin
        }
        illustration {
            ...animatedIconsTargetMixin
        }
    }
}
