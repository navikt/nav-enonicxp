#import "../mixins/animatedIconsTargetFragment.graphql"

fragment contentGuidePageV2 on no_nav_navno_GuidePageV2 {
    dataAsJson
    data {
        area
        audience {
            _selected
            provider {
                provider_audience
            }
        }
        illustration {
            ...animatedIconsTargetMixin
        }
        processing_times
        payout_dates
        rates
    }
}
