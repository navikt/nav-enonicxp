#import "../contentCommonFragment.graphql"

fragment contentPublishingCalendarEntry on no_nav_navno_PublishingCalendarEntry {
    ...contentCommon
    data {
        date
        period
        link {
            _path
        }
    }
}
