#import "../mixins/languagesFragment.graphql"
#import "../mixins/animatedIconsTargetFragment.graphql"
#import "../mixins/audienceFragment.graphql"

fragment contentOverviewPage on no_nav_navno_Overview {
    dataAsJson
    data {
        ...audienceMixin
        languages {
            ...languagesMixin
        }
        illustration {
            ...animatedIconsTargetMixin
        }
        productList(contentId: $ref) {
            _id
            anchorId
            path
            type
            productDetailsPath
            title
            sortTitle
            ingress
            taxonomy
            language
            ...audienceMixin
            area
            illustration {
                type
                data {
                    icons {
                        icon {
                            mediaUrl
                        }
                    }
                }
            }
        }
    }
}
