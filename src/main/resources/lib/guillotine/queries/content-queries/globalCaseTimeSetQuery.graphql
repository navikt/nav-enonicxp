#import "../fragments/contentCommonFragment.graphql"
#import "../fragments/content-types/globalCaseTimeSetFragment.graphql"

query caseProcessingTimeSetQuery($ref: ID!) {
    guillotine {
        get(key: $ref) {
            ...contentCommon
            ...contentGlobalCaseTimeSet
        }
    }
}
