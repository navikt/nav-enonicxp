#import "../fragments/contentCommonWithPageFragment.graphql"
#import "../fragments/content-types/pageMetaFragment.graphql"

query pageMetaQuery($ref: ID!) {
    guillotine {
        get(key: $ref) {
            ...contentCommonWithPage
            ...contentPageMeta
        }
    }
}
