#import "../fragments/contentCommonWithPageFragment.graphql"
#import "../fragments/content-types/officeDataFragment.graphql"

query officeDataQuery($ref: ID!) {
    guillotine {
        get(key: $ref) {
            ...contentCommonWithPage
            ...contentOfficeData
        }
    }
}
