#import "../fragments/contentCommonWithPageFragment.graphql"
#import "../fragments/content-types/situationPageFragment.graphql"

query situationPageV2Query($ref: ID!) {
    guillotine {
        get(key: $ref) {
            ...contentCommonWithPage
            ...contentSituationPage
        }
    }
}
