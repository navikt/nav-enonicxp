#import "../fragments/contentCommonWithPageFragment.graphql"
#import "../fragments/content-types/toolsPageFragment.graphql"

query toolsPageV2Query($ref: ID!) {
    guillotine {
        get(key: $ref) {
            ...contentCommonWithPage
            ...contentToolsPage
        }
    }
}
