#import "../fragments/contentCommonWithPageFragment.graphql"
#import "../fragments/content-types/genericPageFragment.graphql"

query genericPageV2Query($ref: ID!) {
    guillotine {
        get(key: $ref) {
            ...contentCommonWithPage
            ...contentGenericPage
        }
    }
}
