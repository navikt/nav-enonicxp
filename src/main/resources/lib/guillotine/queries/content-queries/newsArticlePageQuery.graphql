#import "../fragments/contentCommonWithPageFragment.graphql"
#import "../fragments/content-types/newsArticlePageFragment.graphql"

query dynamicPageQuery($ref: ID!) {
    guillotine {
        get(key: $ref) {
            ...contentCommonWithPage
            ...contentNewsArticlePage
        }
    }
}
