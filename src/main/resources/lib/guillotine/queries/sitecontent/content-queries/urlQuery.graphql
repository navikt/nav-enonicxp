#import "../fragments/contentCommonFragment.graphql"
#import "../fragments/content-types/urlFragment.graphql"

query ($ref: ID!) {
    guillotine {
        get(key: $ref) {
            ...contentCommon
            ...contentUrl
        }
    }
}
