# ---------- Exposer service ------------
apiVersion: v1
kind: Service
metadata:
  name: xp-service
  namespace: xp
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: xp-app
  ports:
    - name: admin-port
      port: 8080
      targetPort: 8080
    - name: statistics-port
      port: 2609
      targetPort: 2609

---
# ---------- Internal service ------------
apiVersion: v1
kind: Service
metadata:
  name: xp-intra-service
  namespace: xp
  labels:
    app: xp-app
spec:
  clusterIP: None
  selector:
    app: xp-app
  ports:
    - port: 5701
      name: hazelcast
    - port: 9300
      name: elasticsearch
  type: ClusterIP
  publishNotReadyAddresses: true