{
    "name": "navno-enonicxp-tests",
    "version": "1.0.0",
    "scripts": {
        "build": "npm-run-all copy-navno-jar build-image",
        "copy-navno-jar": "cp ../build/libs/navno.jar xp-image",
        "build-image": "docker buildx build ./xp-image -t navno_tests",
        "test": "jest"
    },
    "devDependencies": {
        "@enonic/cli": "3.1.1",
        "npm-run-all": "4.1.5"
    }
}
